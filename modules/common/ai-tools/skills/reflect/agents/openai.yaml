interface:
  display_name: "Reflect"
  short_description: "Extract durable lessons from a completed session"
  default_prompt: "Use $reflect to inspect this session and route reusable lessons into skills or durable memory."
policy:
  allow_implicit_invocation: false
