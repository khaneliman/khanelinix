interface:
  display_name: "To Tickets"
  short_description: "Split a spec into self-contained tickets with dependencies."
  default_prompt: "Use $to-tickets to split this spec into self-contained tickets with dependencies."
policy:
  allow_implicit_invocation: false
