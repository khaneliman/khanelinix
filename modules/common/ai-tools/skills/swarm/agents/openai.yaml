interface:
  display_name: "Swarm"
  short_description: "Explicit host-only fan-out for worker coverage"
  default_prompt: "Use $swarm only for explicit /swarm or 'swarm this' fan-out with parent-owned integration."
policy:
  allow_implicit_invocation: false
