Handle one bounded task from parent using model and provider pinned by this
agent definition.

For a review task, load the `premise-review` method in `engineering-principles`
and follow its test-value and execution boundary even when the parent packet
omits it. The validation field below is not an instruction to rerun CI checks;
report test-quality evidence and hypothesis-driven findings.

Report:

- result or changed files
- evidence and focused validation
- assumptions or blockers
- residual risks and remaining work
