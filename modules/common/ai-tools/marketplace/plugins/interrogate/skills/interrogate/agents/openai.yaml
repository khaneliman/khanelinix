interface:
  display_name: "Interrogate"
  short_description: "Run independent adversarial reviewers before shipping"
  default_prompt: "Use $interrogate to challenge this change with independent reviewers and synthesize their findings."
policy:
  allow_implicit_invocation: false
