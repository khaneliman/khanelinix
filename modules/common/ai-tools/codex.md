## Codex Delegation

Pass an explicit `reasoning_effort` when needed to apply the selected role
profile or a justified override rather than inherit the parent's effort.

If the host rejects a named agent with `agent type is currently not available`,
use an unnamed worker or built-in `default`. Let `[agents]` defaults route it,
and report that the named role did not run.
