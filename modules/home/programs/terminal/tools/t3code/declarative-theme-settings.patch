diff --git a/apps/desktop/src/settings/DesktopClientSettings.test.ts b/apps/desktop/src/settings/DesktopClientSettings.test.ts
index 97f4ca85c..6c77bf3a8 100644
--- a/apps/desktop/src/settings/DesktopClientSettings.test.ts
+++ b/apps/desktop/src/settings/DesktopClientSettings.test.ts
@@ -207,6 +207,43 @@ describe("DesktopClientSettings", () => {
     ),
   );
 
+  it.effect("prefers direct settings when a legacy wrapper remains", () =>
+    withClientSettings(
+      Effect.gen(function* () {
+        const environment = yield* DesktopEnvironment.DesktopEnvironment;
+        const fileSystem = yield* FileSystem.FileSystem;
+        const settings = yield* DesktopClientSettings.DesktopClientSettings;
+        yield* fileSystem.makeDirectory(environment.stateDir, { recursive: true });
+        yield* fileSystem.writeFileString(
+          environment.clientSettingsPath,
+          `{
+            "timestampFormat": "24-hour",
+            "declarativeTheme": {
+              "appearanceMode": "dark",
+              "file": {
+                "version": 1,
+                "id": "managed-theme",
+                "name": "Managed theme",
+                "appearance": "dark",
+                "colors": { "canvas": "#101010" }
+              }
+            },
+            "settings": {
+              "timestampFormat": "12-hour"
+            }
+          }\n`,
+        );
+
+        const persisted = yield* settings.get;
+        assert.isTrue(Option.isSome(persisted));
+        if (Option.isSome(persisted)) {
+          assert.equal(persisted.value.timestampFormat, "24-hour");
+          assert.equal(persisted.value.declarativeTheme?.file.id, "managed-theme");
+        }
+      }),
+    ),
+  );
+
   it.effect("loads defaults from empty client settings documents", () =>
     withClientSettings(
       Effect.gen(function* () {
diff --git a/apps/desktop/src/settings/DesktopClientSettings.ts b/apps/desktop/src/settings/DesktopClientSettings.ts
index 4ff091e27..459a8511d 100644
--- a/apps/desktop/src/settings/DesktopClientSettings.ts
+++ b/apps/desktop/src/settings/DesktopClientSettings.ts
@@ -1,4 +1,8 @@
-import { ClientSettingsSchema, type ClientSettings } from "@t3tools/contracts";
+import {
+  ClientSettingsPatch,
+  ClientSettingsSchema,
+  type ClientSettings,
+} from "@t3tools/contracts";
 import { fromLenientJson } from "@t3tools/shared/schemaJson";
 import * as Context from "effect/Context";
 import * as Crypto from "effect/Crypto";
@@ -17,14 +21,25 @@ const ClientSettingsDocumentSchema = Schema.Struct({
 });
 
 const ClientSettingsJson = fromLenientJson(ClientSettingsSchema);
+const ClientSettingsPatchJson = fromLenientJson(ClientSettingsPatch);
 const LegacyClientSettingsDocumentJson = fromLenientJson(ClientSettingsDocumentSchema);
 const decodeLegacyClientSettingsDocumentJson = Schema.decodeEffect(
   LegacyClientSettingsDocumentJson,
 );
+const decodeClientSettingsPatchJsonValue = Schema.decodeEffect(ClientSettingsPatchJson);
 const decodeClientSettingsJsonValue = Schema.decodeEffect(ClientSettingsJson);
 const decodeClientSettingsJson = (raw: string): Effect.Effect<ClientSettings, Schema.SchemaError> =>
-  decodeLegacyClientSettingsDocumentJson(raw).pipe(
-    Effect.map((document) => document.settings),
+  decodeClientSettingsPatchJsonValue(raw).pipe(
+    Effect.flatMap((patch) =>
+      Object.keys(patch).length > 0
+        ? decodeClientSettingsJsonValue(raw)
+        : decodeLegacyClientSettingsDocumentJson(raw).pipe(
+            Effect.map((document) => document.settings),
+            Effect.catchTags({
+              SchemaError: () => decodeClientSettingsJsonValue(raw),
+            }),
+          ),
+    ),
     Effect.catchTags({
-      SchemaError: () => decodeClientSettingsJsonValue(raw),
+      SchemaError: () => decodeClientSettingsJsonValue(raw),
     }),
   );
 const encodeClientSettingsJson = Schema.encodeEffect(ClientSettingsJson);
diff --git a/apps/web/src/hooks/useSettings.ts b/apps/web/src/hooks/useSettings.ts
index 6eb571f70..97f6d0bd8 100644
--- a/apps/web/src/hooks/useSettings.ts
+++ b/apps/web/src/hooks/useSettings.ts
@@ -48,7 +48,7 @@ import {
   usePrimaryEnvironment,
 } from "~/state/environments";
 import { useAtomCommand } from "~/state/use-atom-command";
-import { useTheme } from "./useTheme";
+import { applyDeclarativeTheme, useTheme } from "./useTheme";
 
 const CLIENT_SETTINGS_PERSISTENCE_ERROR_SCOPE = "[CLIENT_SETTINGS]";
 
@@ -126,7 +126,18 @@ async function hydrateClientSettings(): Promise<void> {
         return;
       }
       if (persistedSettings) {
-        replaceClientSettingsSnapshot({ ...DEFAULT_CLIENT_SETTINGS, ...persistedSettings });
+        const nextSettings = { ...DEFAULT_CLIENT_SETTINGS, ...persistedSettings };
+        if (nextSettings.declarativeTheme) {
+          try {
+            applyDeclarativeTheme(nextSettings.declarativeTheme);
+          } catch (error) {
+            console.error(`${CLIENT_SETTINGS_PERSISTENCE_ERROR_SCOPE} theme sync failed`, {
+              operation: "hydrate-theme",
+              ...safeErrorLogAttributes(error),
+            });
+          }
+        }
+        replaceClientSettingsSnapshot(nextSettings);
       }
     } catch (error) {
       console.error(`${CLIENT_SETTINGS_PERSISTENCE_ERROR_SCOPE} hydrate failed`, {
diff --git a/apps/web/src/hooks/useTheme.ts b/apps/web/src/hooks/useTheme.ts
index 726a03dac..2bf0d77dd 100644
--- a/apps/web/src/hooks/useTheme.ts
+++ b/apps/web/src/hooks/useTheme.ts
@@ -7,9 +7,11 @@ import {
   CUSTOM_THEMES_STORAGE_KEY,
   invalidateCustomThemes,
   canonicalThemePreference,
+  parseThemeFile,
   isKnownThemePreference,
   getThemePreferenceMode,
   parseThemeHalves,
+  replaceCustomThemeCollection,
   resolveDesktopTheme,
   resolveThemeAppearance,
   resolveThemeHalf,
@@ -33,6 +35,11 @@ type ThemeSnapshot = {
   themeHalves: ThemeHalves | null;
 };
 
+type DeclarativeTheme = Readonly<{
+  appearanceMode: ThemePreferenceMode;
+  file: Readonly<Record<string, unknown>>;
+}>;
+
 type DesktopThemeBridge = Pick<DesktopBridge, "setTheme">;
 
 const STORAGE_KEY = "t3code:theme";
@@ -231,6 +238,23 @@ export function writeThemePreference(theme: Theme): void {
   }
 }
 
+export function applyDeclarativeTheme(settings: DeclarativeTheme): void {
+  if (typeof window === "undefined") return;
+
+  const theme = parseThemeFile(settings.file);
+  if (!theme.collection) {
+    throw new Error("Declarative themes must belong to a collection.");
+  }
+
+  replaceCustomThemeCollection(theme.collection.id, [theme]);
+  writeAppearanceModePreference(settings.appearanceMode);
+  writeThemePreference(theme.id);
+
+  lastAppliedTheme = null;
+  applyTheme(theme.id, true);
+  emitChange();
+}
+
 function getStored(): Theme {
   if (themeStorageReadFailure !== null) {
     return DEFAULT_THEME_SNAPSHOT.theme;
diff --git a/packages/contracts/src/settings.test.ts b/packages/contracts/src/settings.test.ts
index 61ad3be41..47ab5c60a 100644
--- a/packages/contracts/src/settings.test.ts
+++ b/packages/contracts/src/settings.test.ts
@@ -169,6 +169,28 @@ describe("ClientSettings panel animations", () => {
   });
 });
 
+describe("ClientSettings declarative theme", () => {
+  it("preserves a versioned theme file and appearance mode", () => {
+    const declarativeTheme = {
+      appearanceMode: "dark" as const,
+      file: {
+        version: 1,
+        id: "managed-theme",
+        name: "Managed theme",
+        appearance: "dark",
+        colors: { canvas: "#101010" },
+      },
+    };
+
+    expect(decodeClientSettings({ declarativeTheme }).declarativeTheme).toEqual(
+      declarativeTheme,
+    );
+    expect(decodeClientSettingsPatch({ declarativeTheme }).declarativeTheme).toEqual(
+      declarativeTheme,
+    );
+  });
+});
+
 describe("ClientSettings environment identification", () => {
   it("defaults to artwork and accepts each presentation mode", () => {
     expect(decodeClientSettings({}).environmentIdentificationMode).toBe("artwork");
diff --git a/packages/contracts/src/settings.ts b/packages/contracts/src/settings.ts
index 9f1f0c846..b4dee5258 100644
--- a/packages/contracts/src/settings.ts
+++ b/packages/contracts/src/settings.ts
@@ -197,6 +197,12 @@ export const BrowserLinkTarget = Schema.Literals(["system", "app"]);
 export type BrowserLinkTarget = typeof BrowserLinkTarget.Type;
 export const DEFAULT_BROWSER_LINK_TARGET: BrowserLinkTarget = "system";
 
+export const DeclarativeTheme = Schema.Struct({
+  appearanceMode: Schema.Literals(["light", "dark", "system"]),
+  file: Schema.Record(Schema.String, Schema.Unknown),
+});
+export type DeclarativeTheme = typeof DeclarativeTheme.Type;
+
 export const ClientSettingsSchema = Schema.Struct({
   appearanceContrast: AppearanceContrast.pipe(
     Schema.withDecodingDefault(Effect.succeed(DEFAULT_APPEARANCE_CONTRAST)),
@@ -261,6 +267,7 @@ export const ClientSettingsSchema = Schema.Struct({
     Schema.withDecodingDefault(Effect.succeed([])),
   ),
   diffIgnoreWhitespace: Schema.Boolean.pipe(Schema.withDecodingDefault(Effect.succeed(true))),
+  declarativeTheme: Schema.optionalKey(DeclarativeTheme),
   diffLayout: DiffLayout.pipe(Schema.withDecodingDefault(Effect.succeed(DEFAULT_DIFF_LAYOUT))),
   environmentIdentificationMode: EnvironmentIdentificationMode.pipe(
     Schema.withDecodingDefault(Effect.succeed(DEFAULT_ENVIRONMENT_IDENTIFICATION_MODE)),
@@ -1130,6 +1137,7 @@ export const ClientSettingsPatch = Schema.Struct({
   confirmThreadUnpin: Schema.optionalKey(Schema.Boolean),
   continueThreadsAfterServerUpdate: Schema.optionalKey(Schema.Boolean),
   diffIgnoreWhitespace: Schema.optionalKey(Schema.Boolean),
+  declarativeTheme: Schema.optionalKey(DeclarativeTheme),
   diffLayout: Schema.optionalKey(DiffLayout),
   environmentIdentificationMode: Schema.optionalKey(EnvironmentIdentificationMode),
   glassOpacity: Schema.optionalKey(GlassOpacity),
