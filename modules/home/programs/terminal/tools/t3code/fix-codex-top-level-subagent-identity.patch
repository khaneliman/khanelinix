diff --git a/apps/server/src/provider/Layers/CodexSessionRuntime.test.ts b/apps/server/src/provider/Layers/CodexSessionRuntime.test.ts
index 3385137a2dae0564b2e7b76bd80d4770b809be6e..05411b09995c14a395674823588b8ed22fde094c 100644
--- a/apps/server/src/provider/Layers/CodexSessionRuntime.test.ts
+++ b/apps/server/src/provider/Layers/CodexSessionRuntime.test.ts
@@ -19,6 +19,7 @@ import {
   makeMemoryConsolidationNotificationFilter,
   openCodexThread,
   readCodexThread,
+  readThreadSpawnSource,
   rollbackCodexThread,
   toMcpElicitationResponse,
 } from "./CodexSessionRuntime.ts";
@@ -694,6 +695,63 @@ describe("makeMemoryConsolidationNotificationFilter", () => {
   });
 });
 
+describe("readThreadSpawnSource", () => {
+  it("uses a top-level role as identity when the source is ordinary", () => {
+    NodeAssert.deepStrictEqual(
+      readThreadSpawnSource({
+        source: { session: "cli" },
+        agentRole: "verifier",
+        parentThreadId: "root-thread",
+      }),
+      {
+        nickname: undefined,
+        role: "verifier",
+        agentPath: undefined,
+        depth: undefined,
+        parentThreadId: "root-thread",
+      },
+    );
+  });
+
+  it("does not treat an ordinary root thread as a spawned child", () => {
+    NodeAssert.equal(
+      readThreadSpawnSource({
+        source: { session: "cli" },
+        parentThreadId: "root-thread",
+      }),
+      undefined,
+    );
+  });
+
+  it("prefers nested spawn identity over conflicting top-level fields", () => {
+    NodeAssert.deepStrictEqual(
+      readThreadSpawnSource({
+        source: {
+          subAgent: {
+            thread_spawn: {
+              agent_nickname: "nested-name",
+              agent_path: "/root/nested-name",
+              agent_role: "nested-role",
+              depth: 1,
+              parent_thread_id: "nested-parent",
+            },
+          },
+        },
+        agentNickname: "top-level-name",
+        agentRole: "top-level-role",
+        parentThreadId: "top-level-parent",
+      }),
+      {
+        nickname: "nested-name",
+        role: "nested-role",
+        agentPath: "/root/nested-name",
+        depth: 1,
+        parentThreadId: "nested-parent",
+      },
+    );
+  });
+});
+
 describe("codexSessionAppServerArgs", () => {
   it("keeps the app-server subcommand when explicit args are provided", () => {
     NodeAssert.deepStrictEqual(codexSessionAppServerArgs(["-c", "model=gpt-5"], undefined), [
diff --git a/apps/server/src/provider/Layers/CodexSessionRuntime.ts b/apps/server/src/provider/Layers/CodexSessionRuntime.ts
index 4ebbf9a2d60211301d326b8c92cd97c7e44de2b0..c588d8269f3b167ff4d5a6e64b3075eb42cde102 100644
--- a/apps/server/src/provider/Layers/CodexSessionRuntime.ts
+++ b/apps/server/src/provider/Layers/CodexSessionRuntime.ts
@@ -984,7 +984,12 @@ function nonEmptyMetadataValue(value: unknown): string | undefined {
   return trimmed.length > 0 ? trimmed : undefined;
 }
 
-function readThreadSpawnSource(thread: { readonly source: unknown }):
+export function readThreadSpawnSource(thread: {
+  readonly source: unknown;
+  readonly agentNickname?: unknown;
+  readonly agentRole?: unknown;
+  readonly parentThreadId?: unknown;
+}):
   | {
       nickname: string | undefined;
       role: string | undefined;
@@ -993,17 +998,31 @@ function readThreadSpawnSource(thread: { readonly source: unknown }):
       parentThreadId: string | undefined;
     }
   | undefined {
+  const topLevelIdentity = {
+    nickname: typeof thread.agentNickname === "string" ? thread.agentNickname : undefined,
+    role: typeof thread.agentRole === "string" ? thread.agentRole : undefined,
+    agentPath: undefined,
+    depth: undefined,
+    parentThreadId:
+      typeof thread.parentThreadId === "string" ? thread.parentThreadId : undefined,
+  };
+  const hasTopLevelIdentity =
+    topLevelIdentity.nickname !== undefined || topLevelIdentity.role !== undefined;
   const source = thread.source;
+  // Some app-server thread responses expose child identity on the Thread
+  // itself even when the source variant is not the nested thread_spawn shape.
+  // Keep that identity available to the Agents surface instead of rejecting
+  // the child and letting the adapter synthesize "general-purpose".
   if (typeof source !== "object" || source === null || !("subAgent" in source)) {
-    return undefined;
+    return hasTopLevelIdentity ? topLevelIdentity : undefined;
   }
   const subAgent = (source as { subAgent: unknown }).subAgent;
   if (typeof subAgent !== "object" || subAgent === null || !("thread_spawn" in subAgent)) {
-    return undefined;
+    return hasTopLevelIdentity ? topLevelIdentity : undefined;
   }
   const spawn = (subAgent as { thread_spawn: unknown }).thread_spawn;
   if (typeof spawn !== "object" || spawn === null) {
-    return undefined;
+    return hasTopLevelIdentity ? topLevelIdentity : undefined;
   }
   const record = spawn as Record<string, unknown>;
   return {
